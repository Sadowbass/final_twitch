<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
   PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-config.dtd"
>

<configuration>
   <properties resource="config.properties" />
   <settings>
      <setting name="localCacheScope" value="STATEMENT" /> <!-- STATEMENT / SESSION -->
   </settings>
   <environments default="development">
      <environment id="development">
         <transactionManager type="JDBC"></transactionManager>
         <dataSource type="POOLED">
       		<property name="poolMaximumCheckoutTime" value="1500"/>
            <property name="poolTimeToWait" value="1500"/>
            <property name="driver" value="${driver}"/>
            <property name="url" value="${url}"/>
            <property name="username" value="${username}"/>
            <property name="password" value="${password}"/>
            <property name="poolMaximumActiveConnections" value="100" />
         </dataSource>
      </environment>
   </environments>
   <mappers>
   	 <mapper resource="mybatis/broadCasting.xml" />
     <mapper resource="mybatis/scbatis.xml"/>
     <mapper resource="mybatis/store.xml"/>
     <mapper resource="mybatis/mybatis_uk.xml"/>
     <mapper resource="mybatis/member.xml"/>
     <mapper resource="mybatis/category.xml"/>
     <mapper resource="mybatis/storeAdmin.xml" />
   </mappers>

</configuration>
